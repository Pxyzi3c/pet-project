import React from 'react'
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Feed | ',
}

const Feed = () => {
    return (
        <section>
            Feed
        </section>
    )
}

export default Feed